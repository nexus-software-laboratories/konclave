[package]
name = "KonclaveSecretStorage"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
rust-version = "1.88"

[features]
default = ["native-keyring"]
native-keyring = ["dep:keyring"]

[dependencies]
aws-lc-rs.workspace = true
keyring = { workspace = true, optional = true }
thiserror.workspace = true
zeroize.workspace = true
